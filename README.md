# sync-notes

手机和电脑双向同步的备忘录。Android 和 Windows 用同一个账号登录，
在任何一端写下的内容，另一端几秒内就能看到；断网时照常读写，联网后自动补齐。

Flutter 一套代码出两端，Supabase 负责登录、存储和实时推送，本地 SQLite 存一份离线副本。

<p align="center">
  <img src="docs/notes-list.png" width="250" alt="笔记列表">
  <img src="docs/note-edit.png" width="250" alt="编辑页">
  <img src="docs/note-rich-text.png" width="250" alt="富文本">
</p>

## 能做到什么

- Android 和 Windows 共用一套代码，同一账号登录即同步
- 本地存完整副本，没网也能看和写，联网后自动上传对账
- 正文是富文本：选中一段字就能加粗、斜体、下划线、改颜色、改字号、加高亮底色
- 单层目录分类，顶部标签一键切换，长按笔记可移动归类
- 笔记里能插图片，图片在文字之间直接显示，两端都能看到
- 一次可以选多张图片插进正文，按选中的顺序一张一行
- 点图片能看大图，滚轮或双指放大，双击切换大小
- 桌面端可以直接把剪切板里的图片粘进笔记（右键菜单里选「粘贴图片」）
- 桌面端可以把文件或选中的文字从别处直接拖进笔记
- 笔记里可以插入手写画布，点开全屏用指头或笔写画
- 可以一次导入多个 txt / markdown 文件，每个文件生成一篇笔记
- 编辑时能一键撤回，误删一段话不用重新打；列表里误删整篇也能撤销
- 单篇笔记可以加口令，忘了口令用登录密码就能关掉加密
- 两端同时改同一篇笔记时保留两份内容，生成带时间戳的「冲突副本」，不互相覆盖
- App 在前台时收到服务端推送，另一端一两秒内出现改动
- 时间戳由服务端生成，设备时钟不准也不影响增量同步
- Postgres 行级权限隔离，每个账号只能读写自己的笔记

还没做的：二级以上目录、表格、分享协作、端到端加密。

**关于加锁**：锁只是应用层的一道门。正文和图片在 Supabase 上仍是明文，
任何拿到你账号的人（或能读数据库的人）都能看到内容。它挡的是「手机被别人拿到、
App 又正好登录着」这种情况。要真正连服务端都读不到的加密，是另一套设计。

## 快速开始

需要 Flutter 3.47 / Dart 3.13 以上，以及一个 Supabase 项目（免费额度足够自用）。

### 1. 建 Supabase 项目

到 [supabase.com](https://supabase.com) 注册并新建项目，然后在
**Settings → API** 拿到两个值：`Project URL`（形如 `https://xxxx.supabase.co`）
和 `anon public` key。

anon key 设计上就是给客户端用的，不是秘密。真正的隔离靠数据库行级权限，
建表脚本里已经配好。

### 2. 建表

打开项目的 **SQL Editor**，新建一个查询，把 [`supabase/schema.sql`](supabase/schema.sql)
全文粘进去执行。脚本可以重复跑，不会破坏已有数据。

它会建 `notes`、`folders`、`note_images` 三张表，打开行级权限，
装一个把更新时间交给服务端时钟的触发器，把前两张表加进实时推送，
再建一个私有的 `note-images` 存储桶并配好归属策略。

> **已经在用的项目**：加了目录、图片、加锁之后服务端结构变了，
> 需要把这份脚本**再执行一次**。脚本是幂等的，老数据不受影响。
> 不执行的话，客户端同步会因为缺列而报错。

### 3. 填配置

把 `config.example.json` 复制一份改名成 `config.json`，填入上面拿到的两个值：

```json
{
  "SUPABASE_URL": "https://xxxx.supabase.co",
  "SUPABASE_ANON_KEY": "eyJhbGciOi..."
}
```

`config.json` 已经在 `.gitignore` 里，不会进版本库。

Supabase 默认要求注册后去邮箱点确认链接才能登录。自己用的话可以到
**Authentication → Sign In / Providers → Email** 把 **Confirm email** 关掉，
省掉这一步。

### 4. 跑起来

```bash
flutter pub get
dart run build_runner build      # 生成本地数据库代码
flutter run -d windows --dart-define-from-file=config.json
flutter run --dart-define-from-file=config.json   # 或者自动选设备
```

改了 `lib/data/local/database.dart` 的表结构之后，要重新跑一次 `build_runner`。

## 构建安装包

Android：

```bash
flutter build apk --release --dart-define-from-file=config.json
```

产物在 `build/app/outputs/flutter-apk/app-release.apk`，用调试密钥签名，适合自己装。
要上架应用商店得另外配正式签名。

Windows：

```bash
flutter build windows --release --dart-define-from-file=config.json
```

产物在 `build/windows/x64/runner/Release/`，是整个文件夹。exe 不能单独拷出来，
它需要同目录的 dll 和 `data` 文件夹。没有代码签名，拷到别的电脑首次运行会被
SmartScreen 拦一下，点「更多信息 → 仍要运行」。

打 Windows 包还需要两个前提，都要管理员权限：Visual Studio 的
「使用 C++ 的桌面开发」工作负载（约 7GB），以及打开系统设置里的开发者模式
（Flutter 创建插件符号链接需要）。装完 `flutter doctor` 里 Visual Studio 那项变成绿色就行。

## 同步是怎么做的

客户端分三层，界面只读本地数据库，不碰网络。断网可用和同步逻辑可测试，都建立在这个前提上。

| 层 | 位置 | 职责 |
| --- | --- | --- |
| 本地库 | `lib/data/local/` | SQLite 存完整镜像，记 `baseVersion` 和脏标记 |
| 远端 | `lib/data/remote/` | Supabase 读写与实时订阅 |
| 同步引擎 | `lib/data/sync/` | 推送、拉取、冲突判定，纯逻辑可单测 |

一轮同步固定「先推后拉」：

1. 把本地带脏标记的笔记按修改时间顺序上传。更新语句带条件
   `version = 上次同步成功时的版本号`，这就是乐观锁，匹配不到行说明期间被别人改过。
2. 匹配不到就转入冲突流程。本地这份内容另存成一条新笔记，标题后加
   「（冲突副本 2026-09-18 14:30）」，原笔记接受服务端的最新版本。两份都留着，
   怎么合并由用户决定。如果撞上的是「本地删、远端改」，则反过来把远端那份留成副本，
   删除照常生效。
3. 拉取按更新时间增量进行，逐条比版本号写入本地。本地还有没推上去的改动就先跳过，
   留给下一轮推送处理，避免把刚写的内容盖掉。
4. App 在前台时订阅服务端推送事件。事件里带设备标识，自己刚写的那条会被过滤掉，
   不做多余的重写。

同步在四个时机触发：App 启动、从后台切回前台、网络恢复，以及前台每 60 秒兜底跑一轮。
最后一条是为了防止实时通道静默断开后长时间不同步。

所有时间戳都由数据库触发器用服务端时间生成，手机和电脑的系统时间不一致也不会
影响增量同步的正确性。

### 目录与图片

推送顺序固定为**目录 → 笔记 → 图片**：笔记带着所属目录的 id，目录得先在服务端存在。
目录没有正文可丢，所以撞车时以本地这次改动为准重试一次，不像笔记那样生成冲突副本。

图片文件走 Supabase Storage 的私有桶，路径第一段是用户 id，策略据此判断归属。
元数据在 `note_images` 表里，内容不可变，所以按 id upsert 就行，不需要乐观锁。

没人引用的图片会回收，分两步走：先挂起 24 小时再标记删除，标记之后再过 7 天
才真正删掉存储桶里的文件。中间的等待是为了躲开同步时序——某个设备刚删掉图片标记，
另一个设备还没拉到那次修改，这时就把文件删了会让人家的图片变成裂图。

## 目录结构

```
lib/data/local/      本地 SQLite 副本（drift）
lib/data/remote/     Supabase 读写与实时订阅
lib/data/sync/       同步引擎与调度
lib/ui/              界面（列表页、编辑页、登录页）
supabase/schema.sql  服务端建表脚本
tool/dev.ps1         开发脚本
test/                测试
```

## 更新日志

从新到旧排列。括号里是构建号和这一版打包的日期（同一天里连着改的几版会共用日期）。
只列用户能感觉到的变化。

### 1.4.4（构建号 19 · 2026-09-20）

- 去掉（安卓）：选区菜单里的「选择」。它会把选区叠加层拆了重建，反而又踩回
  「抓手不出来」那个老毛病。长按拖选本来就能连着选一大段，不需要这个按钮。
- 修复（安卓）：长按之后拖动选字，页面一开始滚动，本该固定不动的那一端就跟着
  内容一起跑，选出来的范围永远一样大。原因是编辑器那套是按**屏幕坐标**重算两端的：
  长按起点存的是当时手指的位置，内容一滚，这个坐标指到的字就变了。现在固定端改记
  「起点是哪个字」（文本偏移），滚多少都不受影响，拖多远就选多远。

### 1.4.3（构建号 18 · 2026-09-20）

- 修复（安卓）：从图片预览、手写画布、相册回来之后输入法还是会弹出来。上一版只是
  打开这些界面前撤掉焦点，但退回来的时候框架会把焦点还给编辑器，编辑器一拿到焦点
  就顺手把键盘唤出来。现在回来之后先撤焦点、收一次键盘，400 毫秒后再看一眼——
  焦点恢复得晚的话就再收一次。
- 修复（安卓）：点文本移动光标时每次都会弹输入法，而不是只有双击才弹。上一版改成
  「把编辑器的请求吞掉」之后，焦点得靠别处补回来，而编辑器一拿到焦点就顺手开了
  输入法连接。现在轻点/拖选结束时把焦点还给编辑器（光标和抓手都靠它显示），紧接着
  下一帧关一次键盘；一次手势只关这一次，不会来回开关。

### 1.4.2（构建号 17 · 2026-09-20）

- 修复（安卓）：拖选、拖抓手的时候输入法「弹出来又收回去」地抽。上一版是发现键盘
  弹了就手动关，结果和编辑器自己的「每次选区变化都要一次键盘」来回较劲，变成开关
  打架。现在改成把它的请求直接吞掉（编辑器自带一个「别要键盘」的开关），一个开关
  动作都不发，自然就不抽了。
- 修复（安卓）：从图片预览、手写画布、相册选图回来时，输入法会自己弹出来。现在
  打开这些页面/选择器之前先让编辑器松手（撤掉焦点），回来它就不会自动把键盘也叫
  起来。
- 修复（安卓）：点「选择」之后文字选中了、两端的拖动抓手还是不出来。上一版只让
  编辑器「重新显示」工具栏和抓手，但叠加层如果还在、抓手已经被摘掉，它是不会补的。
  现在改成先收成一根光标（编辑器会把叠加层整个销毁），下一帧再把这段字选回来，
  叠加层连着两个抓手一起重建。

### 1.4.1（构建号 15–16 · 2026-09-20）

- 修复（安卓）：拖选、拖抓手的时候输入法会一次次往上顶。现在只要选区是「选中一段
  文字」的状态，键盘就会被摁回去；一直没弹过就不去动它，也不会跟双击唤键盘打架。
- 修复（安卓）：「选择」点下去只有选中、抓手还是不出来。抓手要等编辑器真的拿到
  焦点才建得出来，所以改成焦点生效之后（下一帧）再让编辑器把工具栏和抓手一起摆
  一次。顺带把菜单里其它按钮也补上收尾：点完剪切/复制/粘贴/全选之后，编辑器会
  拿回焦点，本来没在打字就不会把键盘又叫出来。

### 1.4.0（构建号 13–14 · 2026-09-20）

- 新增（安卓）：选中文字后弹出的菜单里多了「选择」。长按偶尔只出菜单、不给两端的
  拖动抓手，点它一下重新点亮选区，抓手就回来了；只有光标时点它，会顺带选中光标
  所在的词。
- 修复（安卓）：这条浮动菜单的位置改成算出来的——选区上方放得下就待在格式栏和
  选区之间（给抓手留一截），放不下就翻到选区下方。之前先是挡住格式栏，往下让之后
  又压住了两端的抓手。

### 1.3.0（构建号 12 · 2026-09-20）

- 新增：插图片时可以一次选多张。相册里多选，按选中的先后一张一行接在正文里；
  单张太大或读不出来不会拖累其它几张，插完会提示哪张没进来。
- 修复（安卓）：选中文字后弹出的「剪切 / 复制 / 粘贴 / 全选」会盖住顶上的格式栏。
- 修复（安卓）：页面滚动会回弹。原因是「把光标滚进可视区」的逻辑在手指松开时
  也会跑一次，滚到哪儿都被拽回光标那里。现在只有真的拖选了才滚，单纯翻页不动。
  顺带把边缘自动滚动改得更聪明：拖着选到屏幕边缘后手指停住，也会继续往下滚。
- 修复（安卓）：输入法弹得太勤。现在轻点和拖选只放光标和选区，不叫键盘；
  双击文字才唤起输入法（键盘已经开着时不受影响，接着打字不用再点一次）。

### 1.2.0（构建号 11 · 2026-09-20）

- 新增：点正文里的图片可以看大图。手机双指捏合放大，电脑鼠标滚轮直接滚就能缩放
  （不用按 Ctrl），双击在放大和原始大小之间切换，放大之后拖着平移，
  右上角「适应屏幕」一键还原。
- 修复：插进正文的手写画布过一会儿就点不开了，画上去的笔迹也没了。
  这是个从 1.0.5 就带着的同步 bug：新建画布时漏了「服务端还没有这条」的标记，
  推送时走成了「更新一条不存在的记录」，同步引擎查不到就当成被别的设备删掉，
  把本地这条一起清掉，而笔迹从来没上传过，所以直接丢了。
  现在新建的画布会正常上传；另外同步引擎加了一道保险——本地还没上传过的记录
  就算服务端查不到也补一次插入，不会再当成被删。
  **已经被这个 bug 清掉的手写笔迹找不回来了**，那几个空的手写块只能删掉重画。
- 说明：手写画布本来就不参与版本冲突合并（一幅画没法自动合并），
  撞车时以本地这次改动为准。

### 1.1.1（构建号 10 · 2026-09-20）

- 新增：编辑页右上角多了一个「回到光标」按钮，在撤回左边。光标被翻到看不见的
  地方时，点一下它就跳回屏幕中间，同时把输入焦点还给正文。
- 修复：插完图片或手写画布之后，光标没有稳稳落到下面那一行。插图片前弹的是系统
  文件框、插手写前推的是一个整页，这趟来回之后焦点和选区都可能不在编辑器身上。
  现在插完会明确再要一次焦点、把光标放到块下面那一行、在下一帧再确认一遍，
  并把光标滚进可视区。

### 1.1.0（构建号 8–9 · 2026-09-20）

- 新增：正文换成富文本。选中一段字就能加粗、斜体、下划线、改文字颜色、改字号
  （4 档，最大 32）和加高亮底色，编辑页顶部多了一条格式工具栏。
  工具栏放不下时会自己收进箭头里，窄屏手机上也能用。
- 新增：工具栏上有「清除格式」，选中一段设置过样式的字点它，就退回纯文本的样子。
- 说明：正文的存储格式换成了富文本结构（Quill Delta 的 JSON）。老笔记打开时
  会自动转换并存回去，内容一个字都不会少，但**老版本的 App 打开新笔记会看到一坨
  JSON**。两台设备都升到这一版就行。
- 说明：编辑页的撤回改由编辑器自己记（仍然只在内存里，退出笔记就清空）。
  现在撤回一步能退掉一次输入或一次格式修改，插图片、改样式也在撤回范围内。
  代价是合并窗口从 1.5 秒变成 0.4 秒，连打时的手感略微更碎一点。
- 修复：长文里拖着往下选中时，滚动条会来回抽。原因是编辑器自己那套「把光标滚进
  可视区」的算法把当前滚动位置当成了内容坐标的一部分，偏移越大目标越远，和别的
  滚动调用互相打架。现在编辑器内部不再滚动，正文外面套一层滚动视图由我们驱动，
  选中内容拖到屏幕边缘时也会像别的编辑器那样自动往下滚。
- 修复：插入图片之后接着打字，字会跑到图片上面去。原因是光标停在了图片同一行，
  整行变成「图片 + 文字」排在一行。现在光标落在图片下面那一行，打出来的字就在图下方。
- 修复：手写画布被拉变形、图片被居中这两个排版问题在换成富文本编辑器后重新处理过，
  图片仍然按原始宽高比左对齐显示。

### 1.0.5（构建号 7 · 2026-09-19）

- 新增：笔记里可以插入一块手写画布，点开全屏用指头或笔写画，
  笔迹按归一化坐标存，手机和电脑上显示尺寸不同也不会错位。
  工具里有画笔、橡皮、颜色、粗细、撤销和清空。
- 新增：目录管理有了正经入口。列表右上角菜单里的「管理目录」可以逐项重命名和删除，
  不用再去长按目录标签。
- 修复：手机长按笔记的菜单里没有删除，只能左滑；现在长按会弹出「移动到…／删除」，
  和电脑右键一致。
- 注意：服务端脚本要重新执行一次，多了一张手写画布的表。

### 1.0.4（构建号 6 · 2026-09-18）

- 新增：列表里右键笔记可以「移动到…」，编辑页的菜单里也加了这一项。
  原来换目录只有长按这一个入口，触屏上没问题，用鼠标长按就不像个正常操作了。

### 1.0.3（构建号 4–5 · 2026-09-18）

- 新增：桌面端可以从软件外拖入内容，拖图片文件、拖 txt/md 文件、拖一段选中的文字都行。
- 修复：插入的图片会盖住上下两行文字。输入框默认把行高钉死了，图片撑不开所在的行。
- 修复：在资源管理器里复制的图片粘不进来。构建号 4 只补了原生侧的读取，
  客户端还有个判断顺序的问题，构建号 5 才真正修好。

### 1.0.2（构建号 3 · 2026-09-18）

- 新增：桌面端可以把剪切板里的图片粘进笔记。
- 修复：插入的图片显示成一排「OBJ」方块。

### 1.0.1（构建号 2 · 2026-09-18）

- 新增：单层目录分类、图片插入、txt 与 markdown 导入、编辑撤回、单篇加锁。
- 注意：服务端脚本需要重新执行一次。

### 1.0.0（构建号 1 · 2026-09-18）

- 首个版本：纯文本笔记的手机与电脑双向同步。

### 版本号怎么叠

写在 `pubspec.yaml` 里，格式是 `1.4.4+19`。加号前面是给人看的版本号，加号后面
是给系统看的构建号。

- **修 bug、小改动**：只加最后那位，`1.4.3` → `1.4.4`。
- **加功能**（多了一个能干的事）：中间那位进位、最后一位归零，`1.4.4` → `1.5.0`。
- 最前面那位（`1.x` → `2.x`）留给不兼容的大改，平时不动。

加号后面的构建号每次都要加一，一次不落。Android 就是靠它判断"这是不是新版本"，
数字不变的话新打的包装不上去，会直接提示安装失败。

## 测试

```bash
flutter test
```

109 个测试，另外 1 个真实后端联调测试默认跳过。同步引擎用内存版的本地库和远端来测，
不依赖网络，覆盖笔记的增删改与冲突副本、目录的增删改与归属迁移、图片上传下载与回收、
导入的编码识别、口令派生与校验，以及界面层的目录切换、误删撤销、加锁解锁流程。
富文本部分单独测了老笔记的转换、正文存取往返、内嵌块的插入位置，以及编辑页里
图片按比例左对齐、不压住文字、插图后打字排在图片下方、光标出屏会跟着滚、
拖到窗口边缘会自动滚、翻页之后不弹回光标处，还有安卓上「轻点不弹输入法、
双击才弹」「选区菜单不挡格式栏」这两条。

`test/live_backend_test.dart` 是可选的真实后端联调测试，会真的建两台模拟设备跑一遍
完整同步，包括跨账号隔离。需要显式开启并传入一个专用测试账号（不要用自己日常的账号，
测试结束会清空该账号的笔记）：

```bash
flutter test test/live_backend_test.dart \
  --dart-define=LIVE=true \
  --dart-define-from-file=config.json \
  --dart-define=LIVE_EMAIL=测试邮箱 \
  --dart-define=LIVE_PASSWORD=测试密码
```

## 关于 tool/dev.ps1

项目带了一个 PowerShell 脚本，把依赖、代码生成、检查、打包几条命令包了一遍：

```powershell
.\tool\dev.ps1 get        # 拉依赖
.\tool\dev.ps1 gen        # 生成本地数据库代码
.\tool\dev.ps1 analyze    # 静态检查
.\tool\dev.ps1 test       # 跑测试
.\tool\dev.ps1 apk        # 打 Android 包
.\tool\dev.ps1 win        # 打 Windows 包
.\tool\dev.ps1 run -d windows
```

它是为了绕开开发机上的两个具体问题，普通机器上不需要，直接用上面那些 `flutter` 命令即可。

一是用户名是中文。Dart 的 AOT 编译器（`build_runner` 和 `flutter analyze` 都会用到）
写不了含非 ASCII 字符的路径，会报 `Unable to write file`。脚本建了一个纯英文的目录联接
`D:\work\mod` 指向项目，所有工具从这个路径执行。

二是 `sqlite3` 包在构建时要从 GitHub Releases 下载 .so 和 .dll，国内直连不通。
脚本改用镜像拉取，按官方 SHA-256 校验后放进构建缓存，构建阶段直接复用，不再联网。

脚本开头写死了这台机器的安装位置（Flutter 在 `D:\flutter`、Android SDK 在
`D:\Android\Sdk`），换机器要改那几个变量。

## 其它

Supabase 免费项目连续 7 天没有任何访问会被自动暂停，到面板点一下就能恢复。

手机端只在 App 打开或切回前台时同步，没有做后台常驻推送和通知。

## 许可

MIT，全文见 [LICENSE](LICENSE)。
